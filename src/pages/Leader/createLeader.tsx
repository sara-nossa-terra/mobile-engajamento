import React from 'react';
import { Button } from 'react-native';
import { View, Text } from 'native-base';

const CreateLeader: React.FC = () => {
    return (
        <View>
            <Text>Create leader</Text>
        </View>
    );
};

export default CreateLeader;
